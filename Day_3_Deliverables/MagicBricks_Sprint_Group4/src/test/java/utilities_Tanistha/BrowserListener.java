/*
 * package utilities;
 * 
 * import org.testng.ITestContext; import org.testng.ITestListener;
 * 
 * public class BrowserListener implements ITestListener {
 * 
 * @Override public void onStart(ITestContext context) { String browser =
 * context.getCurrentXmlTest().getParameter("browser"); if (browser != null) {
 * DriverManager.setBrowserForCurrentThread(browser); } } }
 */
package utilities_Tanistha;

